- name: Install Netdata
  shell: bash <(curl -Ss https://my-netdata.io/kickstart.sh)
  args:
    executable: /bin/bash
  become: yes

- name: Ensure Netdata running
  service:
    name: netdata
    state: started
    enabled: yes
  become: yes
